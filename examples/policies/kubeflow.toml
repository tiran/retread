# Midstream fork of kubeflow that adjusts dependency pins, adds integration
# extras, and modifies source files.
["*"]
description = "Midstream fork: source files, dependency pins and extras differ"
ignore_dependency_metadata = true
ignore_differences = [
    "*",
]
