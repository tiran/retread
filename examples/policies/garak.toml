# Downstream builds garak from a modified source fork with updated
# dependency version bounds.
["*"]
description = "Downstream fork: dependency version bounds differ"
ignore_dependency_metadata = true
