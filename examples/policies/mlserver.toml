# Midstream fork of mlserver with adjusted dependency version bounds and
# modified source files.
["*"]
description = "Midstream fork: source files and dependency version bounds differ"
ignore_dependency_metadata = true
ignore_differences = [
    "*",
]
