# Midstream fork of autogluon: its declared dependencies reference the
# matching downstream build versions, adjust some extras, and its source
# files differ.
["*"]
description = "Midstream fork: source files and dependency version pins differ"
ignore_dependency_metadata = true
ignore_differences = [
    "*",
]
